# AGENTS.md

本文件用于指导本项目中“批量读取、筛选、修改结构化文本/游戏数据文件”的脚本编写。  
目标是：**安全、可预测、可回滚、易调试，默认不破坏用户原始数据。**

---

## 一、最高优先级：安全与只读原则

这些规则在任何情况下都不得违反。

- **默认只读**：脚本默认行为必须是只读取、只分析、只输出报告，不得写入任何源文件。
- **显式写入**：任何修改源文件或生成新文件的行为，都必须通过显式参数开启。
- **Dry-run 优先**：所有会写入的脚本都必须支持 `--dry-run`，且在 dry-run 下不得产生任何文件写入、重命名、删除操作。
- **不修改源数据**：除非用户明确选择“原地修改”模式，否则不得改动源目录中的任何文件。
- **可回滚**：原地修改模式应支持自动备份，且备份只生成一次，不覆盖已有备份。
- **最小改动**：只修改用户指定的字段/行/块，其余内容原样保留，包括注释、空行、缩进风格、日期块、额外字段等。
- **不覆盖已有输出**：写入到目标目录（如 mod 目录）时，若目标文件已存在，默认跳过；只有显式指定覆盖时才覆盖。

### 禁止事项

- 禁止默认写入源文件。
- 禁止在 dry-run 下写文件。
- 禁止覆盖已有备份。
- 禁止在未指定输出模式时自动选择原地修改。
- 禁止删除源文件、重命名源文件、移动源文件。
- 禁止修改与目标字段无关的内容。
- 禁止静默跳过严重错误。

---

## 二、工具链与代码规范

- **格式化**：统一使用 `ruff format` 格式化代码。提交前应确保代码通过 `ruff format --check`。
- **Lint**：使用 `ruff check` 检查代码问题，并尽量修复所有警告。
- **类型检查**：使用 `ty check` 进行静态类型检查。所有函数参数和返回值应添加类型注解，尽量通过 `ty` 检查。
- **仅标准库**：脚本运行时只允许使用 Python 标准库，不得引入任何第三方库。
- **Python 版本**：脚本应保持 Python 3.14+ 兼容，只使用 Python 3.14 及以上版本支持的语法和标准库特性；无需考虑 3.14 以下版本的兼容性。
- **不初始化项目环境**：
  - 不执行 `venv`、`uv init`、`poetry init` 等操作。
  - 不生成 `pyproject.toml`、`uv.lock`、`requirements.txt` 等项目管理文件。
  - 脚本应保持单文件或简单脚本形式，可直接用系统 Python 运行。
- **开发工具与运行依赖分离**：
  - `ruff` 和 `ty` 仅作为开发时的外部检查工具，不是脚本运行的前提。
  - 用户运行脚本时，不需要安装这些工具，也不需要激活虚拟环境。
- **命令示例**：
  - 格式化：`ruff format script.py`
  - Lint：`ruff check script.py`
  - 类型检查：`ty check script.py`

---

## 三、命令行接口与运行模式

推荐提供以下通用参数：

- `--source-root` / `--input`：源目录或源文件。
- `--output-mode`：输出模式，至少包含：
  - `list`：只列出将要处理的内容和计划，不写文件。
  - `mod` / `output`：只写入独立输出目录，不碰源文件。
  - `inplace`：原地修改源文件。
- `--dry-run`：模拟运行，不写任何文件。
- `--backup`：原地修改前生成备份。
- `--overwrite` / `--overwrite-output`：允许覆盖输出目录中已存在的文件。
- `--include` / `--exclude`：白名单/黑名单。
- `--include-file` / `--exclude-file`：从文件读取筛选列表。
- `--target` / `--value` / `--factor`：目标值或调整系数。
- `--verbose` / `--quiet`：控制日志详细程度。

参数命名应保持一致，避免同一脚本内混用不同风格。

---

## 四、文件操作规范

- **源文件只读**：`list` 和 `mod` 模式下，源目录必须以只读方式访问。
- **输出目录隔离**：写入独立目录时，输出目录不得与源目录相同；若相同，应警告或拒绝执行。
- **备份策略**：
  - 仅 `inplace` 模式需要备份。
  - 备份文件默认使用 `.bak` 后缀。
  - 若备份已存在，不重复覆盖。
- **编码处理**：
  - 读取时优先使用 `utf-8-sig`，兼容 BOM。
  - 写入时保持 UTF-8。
  - 不得因编码问题丢失原有字符。
- **换行与格式**：
  - 尽量保留原文件换行风格。
  - 只替换目标字段所在行，不重排整个文件。
  - 插入新字段时，插到合理位置，不破坏原有结构。

---

## 五、数据解析与修改逻辑

- **容错解析**：解析器应兼容常见的等价写法，如单行块、多行块、行内注释、空行、多余空格等。
- **只解析必要信息**：不需要完整还原整个语法树；只提取筛选和修改所需的最小信息。
- **不猜测**：无法确定归属或状态时，应跳过并报告，而不是强行修改。
- **保留原始上下文**：修改时基于原始文本行替换，而不是重新序列化整个文件。
- **幂等性**：重复运行同一脚本，结果应一致；不应因为重复运行而不断叠加修改。

---

## 六、筛选与批量处理

- 支持按标识、属性、范围筛选。
- 支持白名单和黑名单。
- 支持从文件读取筛选列表，便于用户维护“只改这些”的清单。
- 筛选逻辑应可组合，且优先级明确。
- 默认处理范围应保守，避免误伤大量数据。

---

## 七、输出与日志

- **明确模式**：每次运行都应打印当前模式、匹配数量、原始总值/目标总值等关键信息。
- **逐项可查**：对每个被处理的条目，打印其标识、旧值、新值。
- **汇总信息**：运行结束打印实际修改数量、跳过数量、错误数量。
- **错误可见**：解析失败、文件不存在、写入失败等情况应明确报错，不得静默忽略。
- **Dry-run 标识**：dry-run 模式下所有输出应明确标注 `[dry-run]`，并说明未写入。

---

## 八、代码风格与可维护性

- 脚本应包含清晰的模块级 docstring，说明用途、模式、参数和示例。
- 函数职责单一：解析、筛选、计算、写入分离。
- 避免硬编码路径、标签、字段名；应通过参数或常量集中管理。
- 使用类型提示和明确的变量命名。
- 对可能失败的操作使用异常处理，并给出可操作的错误信息。

---

## 九、测试与验证建议

- 至少在一个样本文件上验证：
  - `list` 模式不写文件。
  - `mod` 模式不碰源文件。
  - `inplace` + `--backup` 能正确生成备份。
  - `--dry-run` 下无任何写入。
- 对比修改前后文件，确认只有目标字段发生变化。
- 对边界情况测试：空文件、缺少字段、重复字段、单行/多行块、BOM、注释等。

---

遵循以上原则，可以保证此类脚本对用户数据安全、行为可预测、调试方便，并且容易复用到其他类似的数据文件处理任务中。